PORT=4000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/chitwan_rent
JWT_SECRET=dev_secret_change_me
CLIENT_ORIGIN=http://localhost:5173

# Base URL this server is reachable at — used to build the link inside
# verification emails (see routes/verifyListing.js). Defaults to
# http://localhost:$PORT when unset, so local dev works without setting it.
SERVER_BASE_URL=http://localhost:4000

# Gmail address + App Password (not your regular password — generate one at
# https://myaccount.google.com/apppasswords) used to send verification
# emails via Nodemailer (src/lib/email.js).
EMAIL_USER=
EMAIL_PASS=

# Optional — CC'd on every flatmate-match email (src/lib/matchingJob.js) for
# abuse review. Leave unset to send with no CC at all.
MATCH_REVIEW_CC=
